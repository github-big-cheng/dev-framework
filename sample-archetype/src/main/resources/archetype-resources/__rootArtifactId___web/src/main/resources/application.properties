#set( $symbol_pound = '#' )
#set( $symbol_dollar = '$' )
#set( $symbol_escape = '\' )
server.servlet.context-path=/${parentArtifactId}

server.host=localhost
spring.config.import=classpath:application-framework-all.properties
spring.profiles.active=dev
